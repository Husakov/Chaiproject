export class urls{

  url: string;
  name:string;
  description:string;

  constructor(name:string,url:string,desrcirption:string)
  {
  this.name=name;
  this.url=url;
  this.description=desrcirption;
  }
}
